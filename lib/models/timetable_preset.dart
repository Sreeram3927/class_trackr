import 'package:infinity_project/models/timetable_data.dart';

class TimetablePreset {

  String? name;
  TimetableData? data;

  TimetablePreset({
    required this.name,
    required this.data
  });

}